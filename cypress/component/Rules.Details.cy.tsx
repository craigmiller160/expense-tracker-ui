describe('Rule Details', () => {
	it('can add a new rule', () => {
		throw new Error();
	});

	it('can update an existing rule', () => {
		throw new Error();
	});

	it('input validation rules', () => {
		throw new Error();
	});
});
