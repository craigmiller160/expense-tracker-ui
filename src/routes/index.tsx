import { RouteDefinition } from 'solid-app-router';
import { AuthUserContextValue } from '../components/content/AuthUserProvider';

export const createRoutes = (value: AuthUserContextValue): ReadonlyArray<RouteDefinition> => [];