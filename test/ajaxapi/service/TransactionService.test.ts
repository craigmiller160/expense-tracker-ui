export {};

describe('TransactionService', () => {
	describe('requestToQuery', () => {
		it('only required fields', () => {
			throw new Error();
		});

		it('all fields', () => {
			throw new Error();
		});
	});
});
