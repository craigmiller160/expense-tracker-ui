describe('SpendingByCategoryTable', () => {
	describe('sort by functions', () => {
		it('sorts by category', () => {
			throw new Error();
		});

		it('sorts by amount', () => {
			throw new Error();
		});
	});
});
