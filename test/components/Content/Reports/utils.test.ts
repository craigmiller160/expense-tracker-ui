import { getMonthLink } from '../../../../src/components/Content/Reports/utils';

describe('Reports utils', () => {
	it('getMonthLink', () => {
		throw new Error();
	});

	it('getMonthAndCategoryLink', () => {
		throw new Error();
	});
});
