describe('TransactionTable arePropsEqual', () => {
	it('everything is equal', () => {
		throw new Error();
	});

	it('function has changed, everything else is equal', () => {
		throw new Error();
	});

	it('non-function has changed', () => {
		throw new Error();
	});
});
